import { GoogleGenerativeAI } from '@google/generative-ai';
const genAI = new GoogleGenerativeAI('AIzaSyAfg1I0n3HIBGpNoe8Gc-6bhjY1Y4T7EkU');
async function run() {
  try {
    // Wait, the SDK might not have listModels easily exposed. 
    // Let's just use fetch.
  } catch(e) {}
}
